import React, { Component } from "react"
import { render } from "react-dom"

export function renderControl(controlPath, elementId){
    render(
        <Test result="Passed" />,
        document.getElementById        
    )
}