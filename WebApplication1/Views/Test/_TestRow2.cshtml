@model TestModel2
@{ 
    var collectionName = ViewData["CollectionName"] as string;
    Response.Headers.Add("Dynamic-Row-Target", "tbody > *");
}

<table>
    <tbody>
        @using (Html.BeginCollectionItem(collectionName))
        {
            @Html.EditorFor(m => m)
        }
    </tbody>
</table>

